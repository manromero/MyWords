* make icon click surface bigger.
* use hooks when call to firestore
* use hook auth instead of firebase auth service

## Next Features
* update modal action-bottom-sheet

## Generate Debug apk: 

npx react-native build-android --mode=release
cd android
./gradlew assembleRelease